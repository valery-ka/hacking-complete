import { VerseConfig } from "../verse.types";
import * as O from "./objects";

export const verse44: VerseConfig = {
    camera: O.camera,
    light: O.light,
    shadows: O.shadows,
    ground: O.ground,
    walls: O.walls,
    effects: O.effects,
    player: O.player,
    enemies: O.enemies,
    settings: O.settings,
    triggers: O.triggers,
    music: O.music,
};
