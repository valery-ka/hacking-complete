import { ShadowsConfig } from "types/engine/Shadows.types";

export const shadows: ShadowsConfig = {
    orthoBounds: 200,
    dynamicMapSize: 4096,
    staticMapSize: 2048,
    blurKernel: 8,
    blurScale: 1,
};
